CONTROLLER_RACING = string(default='GapFollower')			# controller used for racing
CONTROLLER_OBSTACLE = string(default='DisparityExtender')		# controller used for obstacle avoidance
MAX_LIDAR_DIST = integer(min=0, default=3000000)			# maximum distance for the LiDAR ranges used to exclude outliers
PREPROCESS_CONV_SIZE = integer(min=0, default=3)			# convolution window for preprocessing the LiDAR measurements
MAX_SCAN_DIFF = float(min=0, default=1)					# maximum difference between LiDAR scans used to classify obstacles
SWITCH_FRAMES = integer(min=1, default=10)				# number of control cycles in which the control inputs are interpolated during switching
MAX_STEER = float(min=0.0, default=0.1)					# maximum steering angle for switching
