DIFFERENCE_THRESHOLD = float(min=0.0, default=2.0)		# the minimum difference between adjacent LiDAR points for us to call them disparate
SPEED = float(default=5.0)					# speed of the car
SAFETY_PERCENTAGE = float(min=100.0, default=300.0)             # increase of the car's width to robustly avoid obstacles
STEER_CORRECTION = float(min=0.0, default=1.0)			# correction of the steering angle
LIDAR_RANGE = integer(min=1, default=135)			# number of lidar beams excluded on each side
