BUBBLE_RADIUS = integer(min=0, default=160)
PREPROCESS_CONV_SIZE = integer(min=0, default=3)
BEST_POINT_CONV_SIZE = integer(min=0, default=80)
MAX_LIDAR_DIST = integer(min=0, default=3000000)
STRAIGHTS_SPEED = float(default=9.0)
CORNERS_SPEED = float(default=8.0)
STRAIGHTS_STEERING_ANGLE = float(default=10.0)
