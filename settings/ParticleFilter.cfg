PARTICLES = integer(min=0, default=50)			# number of particles
SIGMA_POSITION = float(min=0.0, default=0.1)		# standard deviation of the normal distribution for the position
SIGMA_ORIENTATION = float(min=0.0, default=0.05)	# standard deviation of the normal distribution for the orientation
