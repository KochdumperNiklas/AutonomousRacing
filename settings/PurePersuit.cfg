LOOKAHEAD_DIST = float(min=0.0, default=1.0)		# lookahead distance to select best raceline point
VELOCITY_GAIN = float(min=0.0, default=1.0)		# gain for the raceline velocity
MAX_REACQUIRE = float(min=0.0, default=20.0)		# maximum distance at which we aim to follow the raceline
ADAPTIVE_CRUISE_CONTROL = boolean(default=False)	# use adaptive cruise control to safely drive behind other cars 
