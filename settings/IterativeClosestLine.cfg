MAX_LIDAR_DIST = float(min=0.0, default=30.0)			# maximum distance for the LiDAR ranges used to exclude outliers
MIN_POINTS = integer(min=0, default=20.0)			# minimum number of points for a line segment
EMPTY_DIST = float(min=0.0, default=0.5)			# minimum free space for a line segment to be disregarded
MAX_DIST_POSITION = float(min=0.0, default=0.5)			# maximum distance between two position estimates
MAX_DIST_ORIENTATION = float(min=0.0, default=0.5)		# maximum distance between two orientation estimates
